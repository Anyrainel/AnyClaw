{
  "name": "anyclaw-server",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b packages/shared packages/dispatch packages/tunnel-manager packages/app-backend packages/app-frontend packages/frontend-template packages/direct-connect packages/wireguard",
    "test:ci": "npm run build && npm run typecheck && npm test",
    "lint": "echo \"(lint wired in later plans)\""
  },
  "devDependencies": {
    "@types/node": "^22.15.14",
    "typescript": "^5.9.3",
    "vitest": "^4.1.5"
  }
}
